2026-03-19T01:36:50.854Z	commentary	I’m reviewing the project context first so I can make the smallest change that satisfies `ST-013`, including any required schema/migration work and the UI surface for due dates/overdue state.
2026-03-19T01:37:01.808Z	commentary	I’ve confirmed the spec already expects `dueDate` in the todo data model, so I’m checking how todos currently flow through the model, API, and UI before I patch the smallest set of files.
2026-03-19T01:37:10.925Z	commentary	I found the existing model already has a `dueDate` column path through create/update, so the main gap is likely overdue detection plus clearer UI and any missing validation/query support. I’m reading the rest of the todo implementation and current tests before editing.
2026-03-19T01:37:34.353Z	commentary	The todo schema already includes `due_date`, so this isn’t a new migration unless I find a missing column mismatch. The remaining work looks like domain logic in a shared helper plus API/UI changes so overdue state is explicit and due dates are editable after creation.
2026-03-19T01:38:50.031Z	commentary	I found a timezone/semantics problem with the current `timestamptz` due date path: a `type="date"` value becomes a midnight UTC timestamp, which can render as the previous day and mark items overdue too early. I’m adjusting the model to treat due dates as dates, then wiring that through API/UI with explicit overdue state.
2026-03-19T01:38:55.457Z	commentary	Plan:\n1. In progress: add shared deadline utilities and update the todo domain/API contract to expose overdue state and validate due dates as dates.\n2. Pending: add minimal UI support to display overdue status clearly and let authenticated users update a todo’s due date.\n3. Pending: run required migration/build/test/lint validation and confirm the final behavior.
2026-03-19T01:39:49.844Z	commentary	I have enough context now. The concrete changes are: convert due dates to a true date model, add shared deadline helpers plus an API overdue filter, and make the todo card support inline due-date edits with a clear overdue indicator.
2026-03-19T01:41:35.983Z	commentary	I’m generating the migration now because the clean fix is to model `due_date` as a date instead of a timestamp. After that I’ll patch the domain/UI code against the final schema shape.
2026-03-19T01:42:40.397Z	commentary	I’m editing the shared deadline helper, todo model/API, the todo card UI, and the generated migration in one pass so the behavior stays consistent end to end.
2026-03-19T01:44:01.317Z	commentary	The main patch is in place. I’m checking the edited files for type/lint issues and a couple of edge cases around the inline due-date control before I run migrations and project validation.
2026-03-19T01:44:20.563Z	commentary	I’ve verified the patch shape. Next I’m running the migration workflow against the local database, then lint/type/build so I can catch any integration issues before closing this out.
2026-03-19T01:44:33.190Z	commentary	The database isn’t reachable on `127.0.0.1:5433`, so migration execution and status verification are blocked by the environment rather than the code. I’m still running the static validations now so the implementation itself is checked.
2026-03-19T01:44:48.955Z	commentary	Lint is blocked by an older generated migration with unused `pgm` parameters, not by the deadline changes. I’m patching that dead boilerplate so the repository can pass its own lint gate cleanly.
2026-03-19T01:45:26.482Z	commentary	The lint rule doesn’t ignore underscore-prefixed parameters in this repo. I’m waiting for the build to finish, then I’ll remove those unused args outright and rerun lint once.
2026-03-19T01:46:25.531Z	commentary	I’ve removed the last repo-level lint blocker. I’m rerunning lint and build once so the final validation reflects the cleaned tree, not the earlier warning-only run.
2026-03-19T01:47:28.871Z	commentary	Validation is complete except for the database-backed checks, which remain blocked by the missing Postgres process. I’m doing a final pass over the changed files and workspace status, then I’ll summarize exactly what changed and what still needs a running DB.
2026-03-19T01:48:28.393Z	final_answer	Implemented `ST-013` by moving deadline handling onto a shared date-based utility in [src/lib/deadlines.ts](/workspace/output/todo-app/src/lib/deadlines.ts#L1), using it to validate/store todo `dueDate` values as calendar dates in [src/models/todo.ts](/workspace/output/todo-app/src/models/todo.ts#L126), and adding overdue filtering support in [src/api/todos.ts](/workspace/output/todo-app/src/api/todos.ts#L40). The schema change is in [src/models/migrations/1773884497247_due-date-as-date.js](/workspace/output/todo-app/src/models/migrations/1773884497247_due-date-as-date.js#L11), which converts `todos.due_date` from `timestamptz` to `date` so date-only inputs no longer drift by timezone.\n\nThe `/todos` UI now shows due dates and overdue state clearly, and authenticated users can update or clear a due date inline from each card via [src/components/TodoItem.tsx](/workspace/output/todo-app/src/components/TodoItem.tsx#L41) and [src/components/TodoList.tsx](/workspace/output/todo-app/src/components/TodoList.tsx#L411).\n\nValidation:\n- `npm test`: passed, but the script only reports `No tests configured yet`.\n- `npm run typecheck`: passed.\n- `npm run lint`: passed.\n- `npm run build`: passed.\n- `npm run db:migrate`: failed because Postgres was unavailable at `127.0.0.1:5433` (`ECONNREFUSED`).\n- `npm run db:migrate:status`: failed for the same reason.\n- Manual DB-backed validation was not possible until the local Postgres instance is running.
