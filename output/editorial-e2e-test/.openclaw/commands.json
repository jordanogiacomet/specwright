{
  "commands": {
    "dev": "npm run dev",
    "build": "npm run build",
    "lint": "npm run lint",
    "test": "npm test",
    "typecheck": "npx tsc --noEmit",
    "db_start": "docker compose up -d postgres",
    "db_stop": "docker compose down",
    "db_migrate": "npx payload migrate",
    "db_seed": "npx payload seed"
  },
  "setup": {
    "install": "npm install",
    "env": "cp .env.example .env.local",
    "db_start": "docker compose up -d postgres",
    "db_migrate": "npx payload migrate"
  },
  "notes": [
    "These commands are pre-populated from the project stack. Update them if the actual project setup differs.",
    "Run 'build' and 'lint' after every story. Run 'test' when test files exist.",
    "Run 'typecheck' periodically to catch type errors early."
  ]
}
