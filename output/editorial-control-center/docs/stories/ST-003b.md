# ST-003b — Initialize project repository (part 2 of 2)

**Story key:** `bootstrap.repository-part-2`

## Description

Create project structure using nextjs + payload + postgres.

## Acceptance Criteria

- [ ] Vitest is installed and configured so that `npm test` runs a real test runner, not a no-op
- [ ] At least one smoke test exists (e.g. a test that imports the app config and asserts it loads)
- [ ] npm install completes without errors
- [ ] npm run build passes
- [ ] npm test passes (must not be a no-op)

## Scope Boundaries

- Do NOT implement any features yet — this is project scaffolding only
- Do NOT add authentication, roles, or any business logic
- Do NOT create database tables — that is a separate story
- Do NOT create files in src/pages/ — this project uses the App Router (src/app/) exclusively
- Use environment variable names exactly as defined in .env.example — do NOT rename or create alternatives

## Expected Files

- `.gitignore`
- `README.md`
- `vitest.config.ts`
- `src/__tests__/smoke.test.ts`

## Dependencies

- `bootstrap.repository`

## Validation

- Run: `npm install`
- Run: `npm run build`
- Run: `npm run lint`
- Run: `npm test`
- Manual: Project directory exists with expected structure; npm test runs a real test suite
